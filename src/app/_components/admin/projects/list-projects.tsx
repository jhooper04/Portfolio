"use client";
import type { AdminPageProps } from "app/_components/admin/common";

const ListProjectsAdmin: React.FunctionComponent<AdminPageProps> = ({client}) => {
    
    return (
        <div>
            <h1>Projects</h1>
            
            <p>here is the projects</p>
        </div>
    );
}

export default ListProjectsAdmin;