"use client";
import type { AdminPageProps } from "app/_components/admin/common";

const ListUsersAdmin: React.FunctionComponent<AdminPageProps> = ({client}) => {
    
    return (
        <div>
            <h1>Users</h1>
            
            <p>here is the users</p>
        </div>
    );
}

export default ListUsersAdmin;